# Copy to .env and edit
ADMIN_PASSWORD=change_me_now
# Absolute path to persistent data dir (e.g., /data on Render, or ./data locally)
DATA_DIR=
# Node port (Render ignores if using default)
PORT=3000
